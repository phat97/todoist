import React from "react";

/** Implicit Return instead of writing
 * () => {
 * return (...);
 * }
 */

export const App = () => (
  <div className="App">
    <header className="App-header">
      <p>Hello World</p>
    </header>
  </div>
);
